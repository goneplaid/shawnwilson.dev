export type ExperienceContent = {
  id: string;
  company: string;
  jobTitle: string;
  duration: string;
  description: string;
};

export type ProjectContent = {
  id: string;
  project: string;
  year: string;
  description: string;
  tools: string;
};
